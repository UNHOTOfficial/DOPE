import React from "react";

export default function Sizes({ sizes }: any) {
  return (
    <div>
      {/* {sizes.length > 1 ? (
        sizes.map((size: any) => (
          <span
            key={size}
            className="px-2 ring-1 ring-gray-100 dark:ring-gray-700 rounded mx-1"
          >
            {size}
          </span>
        ))
      ) : (
        <span
          key={sizes[0]}
          className="px-2 ring-1 ring-gray-100 dark:ring-gray-700 rounded mx-1"
        >
          {sizes[0]}
        </span>
      )} */}
    </div>
  );
}
